{
  "name": "durak-game-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "cd apps/frontend && npm run build",
    "dev": "cd apps/frontend && npm run dev"
  }
}