{
  "name": "durak-game-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build:frontend": "npm run build --workspace=apps/fronted",
    "dev:frontend": "npm run dev --workspace=apps/fronted",
    "install:frontend": "npm install --workspace=apps/fronted"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vite": "^6.3.5"
  }
}
