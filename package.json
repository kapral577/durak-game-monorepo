{
  "name": "durak-game-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "cd apps/fronted && npm install && npm run build",
    "dev": "cd apps/fronted && npm run dev",
    "install:frontend": "cd apps/fronted && npm install"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
